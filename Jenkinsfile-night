pipeline {
        agent any
        stages {
            stage('Docker image'){
                steps{
                    sh 'docker build -f aws/Dockerfile -t build-image .'
                    sh 'docker build -f custom-build/Dockerfile -t custom-aws .'
                }
            }
        }
}

