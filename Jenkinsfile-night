pipeline {
        agent any
        stages {
            stage('Docker image'){
                steps{
                    sh 'docker build -t custom-build .'
                }
            }
        }
}